<UserControl x:Class="IR.ResizeModule.Views.ResizeCommandsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel Orientation="Horizontal">
        <Button Command="{Binding ResizeFileCommand}" Template="{StaticResource CommandButtonTemplate}" Cursor="Hand"
                Margin="5 0 0 0">
            Resize image
        </Button>
        <Button Command="{Binding ResizeFolderCommand}" Template="{StaticResource CommandButtonTemplate}" Cursor="Hand"
                Margin="5 0 0 0">
            Resize folder
        </Button>
    </StackPanel>
</UserControl>
